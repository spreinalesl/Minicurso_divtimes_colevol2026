mrca(tree)
tree <- read.tree("../posterior_maxcred.tre")
tree <- read.nexus("../posterior_maxcred.tre")
tree
plot(tree)
tree <- read.nexus("../posterior_maxcred.tre")
plot(tree)
## Leer es arbol inicial
tree <- read.nexus("data/posterior_maxcred.tre")
tree <- read.tree("data/startingTree.newick")
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
tree$edge.length
plot.phylo(x=ladderize(phy=tree, right=FALSE), cex = 0.6)
# determine the node IDs to calibrate
carangidae_id <- mrca(tree)["Gnathanodon_speciosus", "Caranx_caninus"]
centropomidae_id <- mrca(tree)["Centropomus_mexicanus", "Centropomus_armatus"]
istiophoridae_id <- mrca(tree)["Xiphias_gladius", "Istiophorus_platypterus"]
latidae_id <- mrca(tree)["Lates_japonicus", "Hypopterus_macropterus"]
sphyraenidae_id <- mrca(tree)["Sphyraena_jello", "Sphyraena_helleri"]
root_id <- mrca(tree)["Nematistius_pectoralis", "Sphyraena_waitii"]
plot.phylo(x=ladderize(phy=tree, right=FALSE), cex = 0.6)
nodelabels()
carangidae_id
centropomidae_id
plot.phylo(x=ladderize(phy=tree, right=FALSE), cex = 0.6)
nodelabels(cex=0.6)
plot.phylo(x=ladderize(phy=tree, right=FALSE), cex = 0.6)
nodelabels(cex=0.6, frame = "circle", bg = "white")
istiophoridae_id
latidae_id
sphyraenidae_id
# determine the node IDs to calibrate
(centropomidae_id) <- mrca(tree)["Centropomus_mexicanus", "Centropomus_armatus"]
library(tbea)
?findParams
?c_truncauchy
library(tbea)
library(mcmc3r)
library(phangorn)
## Read tree
tre <- read.tree("data/posterior_maxcred.tre")
## Leer es arbol inicial
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
?write.tree
## Leer es arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
getwd()
## Leer es arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Escribir el arbol en Newick
write.tree(phy=tree, file="startingTree.newick")
## Leer el arbol el Newick
tree <- read.tree("data/startingTree.newick")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Obtener informacion del arbol
print(tree)
is.rooted(tree)
is.binary(tree)
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Add node labels
tree$Nnode # 62 nodes - 63 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode) # number nodes
tree$node.label <- paste("n", nodesnm, sep ="")
tree$node.label
## Plotar el arbol
plot.phylo(x=ladderize(phy=tree, right=FALSE),
cex=0.7, label.offset=0.02)
nodelabels(cex=0.55, frame="circle", bg="white")
tree$node.label
## Escribir el arbol en Newick
write.tree(phy=tree, file="data/startingTree.newick")
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
library(tbea)
library(mcmc3r)
library(phangorn)
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Obtener informacion del arbol
print(tree)
is.rooted(tree)
is.binary(tree)
## Plotar el arbol
plot.phylo(x=ladderize(phy=tree, right=FALSE),
cex=0.7, label.offset=0.02)
nodelabels(cex=0.55, frame="circle", bg="white")
tree$edge.length <- NULL
tree
### Definir IDs para los nodos a ser calibrados
sphyraenidae_id <- mrca(tree)["Sphyraena_jello", "Sphyraena_helleri"]
print(sphyraenidae_id)
carangidae_id <- mrca(tree)["Gnathanodon_speciosus", "Caranx_caninus"]
print(carangidae_id)
istiophoridae_id <- mrca(tree)["Xiphias_gladius", "Istiophorus_platypterus"]
print(istiophoridae_id)
root_id <- mrca(tree)["Nematistius_pectoralis", "Sphyraena_waitii"]
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
tree$edge.length
tree$edge.length[sphyraenidae_id]
tree$edge.length
tree$edge.length[sphyraenidae_id]
sphyraenidae_id
tree$edge.length[sphyraenidae_id] <- "LN(2.3,3.0)"
tree$edge.length
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Obtener informacion del arbol
print(tree)
is.rooted(tree)
is.binary(tree)
## Plotar el arbol
plot.phylo(x=ladderize(phy=tree, right=FALSE),
cex=0.7, label.offset=0.02)
nodelabels(cex=0.55, frame="circle", bg="white")
sphyraenidae_id <- mrca(tree)["Sphyraena_jello", "Sphyraena_helleri"]
print(sphyraenidae_id)
carangidae_id <- mrca(tree)["Gnathanodon_speciosus", "Caranx_caninus"]
print(carangidae_id)
istiophoridae_id <- mrca(tree)["Xiphias_gladius", "Istiophorus_platypterus"]
print(istiophoridae_id)
root_id <- mrca(tree)["Nematistius_pectoralis", "Sphyraena_waitii"]
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
calib
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "L(0.72,0.001,0.01157324,0.025)"
calib$calibstring[which(calib$id == "62")] <- "SN(0.4502984,0.05403769,0.0)"
?paste0
### Escribir el árbol de inicio con toda la información necesaria
for(i in calib$id){
tree$edge.length[i] <- paste0("'", calib$calibstring[i], "'")
}
tree$edge.length
calib$id
calib
calib$calibstring[which(calib$id == "56")] <- "undefined"
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
tree$edge.length
### Escribir el árbol de inicio con toda la información necesaria
for(i in calib$id){
tree$edge.length[i] <- paste0("'", calib$calibstring[i], "'")
}
tree$edge.length
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
### Escribir el árbol de inicio con toda la información necesaria
for(i in calib$id){
tree$edge.length[i] <- paste0("'", calib$calibstring[which(calib$id) == i], "'")
}
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
### Escribir el árbol de inicio con toda la información necesaria
for(i in calib$id){
tree$edge.length[i] <- paste0("'", calib$calibstring[which(calib$id == i)], "'")
}
tree$edge.length
tree$edge.length <- NULL
tree$edge.length
1:length(tree$edge.length)[-calib$id]
blen <- 1:length(tree$edge.length)[-c(calib$id)]
blen
1:length(tree$edge.length)
length(tree$edge.length)
tree$edge.length
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
1:length(tree$edge.length)
[-c(calib$id)]
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
### Escribir el árbol de inicio con toda la información necesaria
tree$edge.length <- ""
for(i in calib$id){
tree$edge.length[i] <- paste0("'", calib$calibstring[which(calib$id == i)], "'")
}
tree$edge.length
write.tree(phy = calibrated, file = "startingTree.newick")
write.tree(phy = tree, file = "startingTree.newick")
?phangorn::write.tree
??write.tree
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Add node labels
tree$Nnode #
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
tree
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
tree
## Add node labels
tree$Nnode #
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
tree
## Plotar el arbol
plot.phylo(x=ladderize(phy=tree, right=FALSE),
cex=0.7, label.offset=0.02)
nodelabels(cex=0.55, frame="circle", bg="white")
### Escribir el árbol de inicio con toda la información necesaria
for(i in calib$id){
tree$node.label[i] <- paste0("'", calib$calibstring[which(calib$id == i)], "'")
}
tree$node.label
### Escribir el árbol de inicio con toda la información necesaria
calib$id <- paste("n", calib$id, sep ="")
calib
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
### Escribir el árbol de inicio con toda la información necesaria
calib$id <- paste("n", calib$id, sep ="")
for(i in calib$id){
tree$node.label[i] <- paste0("'", calib$calibstring[which(calib$id == i)], "'")
}
tree$node.label
calib
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
### Definir IDs para los nodos a ser calibrados
sphyraenidae_id <- mrca(tree)["Sphyraena_jello", "Sphyraena_helleri"]
print(sphyraenidae_id)
carangidae_id <- mrca(tree)["Gnathanodon_speciosus", "Caranx_caninus"]
print(carangidae_id)
istiophoridae_id <- mrca(tree)["Xiphias_gladius", "Istiophorus_platypterus"]
print(istiophoridae_id)
root_id <- mrca(tree)["Nematistius_pectoralis", "Sphyraena_waitii"]
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "L(0.72,0.001,0.01157324,0.025)"
calib$calibstring[which(calib$id == "62")] <- "SN(0.4502984,0.05403769,0.0)"
calib$calibstring[which(calib$id == "56")] <- "undefined"
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
tree$node.label
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
calib
### Escribir el árbol de inicio con toda la información necesaria
calib$id <- paste("n", calib$id, sep ="")
calib$id
for(i in calib$id){
tree$node.label[i] <- paste0("'", calib$calibstring[which(calib$id == i)], "'")
}
tree$node.label
?write.tree
phangorn::write.tree(phy=tree, file="startingTree.newick", format="phylip")
write.tree(phy=tree, file="startingTree.newick", format="phylip")
write.tree(phy=tree, file="startingTree.phy")
## Plotar el arbol
plot.phylo(x=ladderize(phy=tree, right=FALSE),
cex=0.7, label.offset=0.02)
nodelabels(cex=0.55, frame="circle", bg="white")
tree$node.label
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
sphyraenidae_id <- mrca(tree)["Sphyraena_jello", "Sphyraena_helleri"]
print(sphyraenidae_id)
carangidae_id <- mrca(tree)["Gnathanodon_speciosus", "Caranx_caninus"]
print(carangidae_id)
istiophoridae_id <- mrca(tree)["Xiphias_gladius", "Istiophorus_platypterus"]
print(istiophoridae_id)
root_id <- mrca(tree)["Nematistius_pectoralis", "Sphyraena_waitii"]
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "L(0.72,0.001,0.01157324,0.025)"
calib$calibstring[which(calib$id == "62")] <- "SN(0.4502984,0.05403769,0.0)"
calib$calibstring[which(calib$id == "56")] <- "undefined"
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
### Escribir el árbol de inicio con toda la información necesaria
calib$id <- paste("n", calib$id, sep ="")
for(i in calib$id){
tree$node.label[i] <- paste0("'", calib$calibstring[which(calib$id == i)], "'")
}
tree$node.label
i
tree$node.label[i]
calib
i <- calib$id[1]
i
tree$node.label[i]
i <- calib$id[2]
tree$node.label[i]
i <- calib$id[3]
tree$node.label[i]
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
calib
tree$node.label
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
tree$node.label
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
for(i in calib$id){
tree$node.label[i] <- gsub(i, calib$calibstring[which(calib$id == i)], tree$node.label)
}
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
calib
calib$calibstring[which(calib$id == "56")] <- "'undefined'"
calib$calibstring[which(calib$id == "62")] <- "'SN(0.4502984,0.05403769,0.0)'"
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "'L(0.72,0.001,0.01157324,0.025)'"
for(i in calib$id){
tree$node.label[which(tree$node.label == i)] <- calib$calibstring[which(calib$id == i)]
}
tree$node.label
write.tree(phy=tree, file="startingTree.phy")
getwd()
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "'L(0.72,0.001,0.01157324,0.025)'"
calib$calibstring[which(calib$id == "62")] <- "'SN(0.4502984,0.05403769,0.0)'"
calib$calibstring[which(calib$id == "56")] <- "'undefined'"
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
## unfortunately write.tree change "," to "-", so we have to
## change "," to another separator such as "@"
calib$id <- paste("n", calib$id, sep ="")
calib$calibstring
calib
calib$calibstring <- gsub(pattern=",", replacement="@")
calib$calibstring <- gsub(pattern=",", replacement="@", calib$calibstring)
calib
### Escribir el árbol de inicio con toda la información necesaria
for(i in calib$id){
tree$node.label[which(tree$node.label == i)] <- calib$calibstring[which(calib$id == i)]
}
tree$node.label
write.tree(phy=tree, file="startingTree.phy")
calib
calib$calibstring <- gsub(pattern="(|)", replacement="&", calib$calibstring)
calib
gsub
?gsub
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "'L(0.72,0.001,0.01157324,0.025)'"
calib$calibstring[which(calib$id == "62")] <- "'SN(0.4502984,0.05403769,0.0)'"
calib$calibstring[which(calib$id == "56")] <- "'undefined'"
## unfortunately write.tree change "," to "-", so we have to
## change "," to another separator such as "@"
calib$id <- paste("n", calib$id, sep ="")
calib
calib$calibstring <- gsub(pattern=",", replacement="@", calib$calibstring)
calib$calibstring <- gsub(pattern="()", replacement="&", calib$calibstring)
calib
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "'L(0.72,0.001,0.01157324,0.025)'"
calib$calibstring[which(calib$id == "62")] <- "'SN(0.4502984,0.05403769,0.0)'"
calib$calibstring[which(calib$id == "56")] <- "'undefined'"
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
## unfortunately write.tree change "," to "-", so we have to
## change "," to another separator such as "@"
calib$id <- paste("n", calib$id, sep ="")
write.tree(phy=tree, file="startingTree.phy")
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "'L[0.72-0.001-0.01157324-0.025]'"
calib$calibstring[which(calib$id == "62")] <- "'SN(0.4502984,0.05403769,0.0)'"
calib$calibstring[which(calib$id == "56")] <- "'undefined'"
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
## unfortunately write.tree change "," to "-", so we have to
## change "," to another separator such as "@"
calib$id <- paste("n", calib$id, sep ="")
tree$node.label
### Escribir el árbol de inicio con toda la información necesaria
for(i in calib$id){
tree$node.label[which(tree$node.label == i)] <- calib$calibstring[which(calib$id == i)]
}
tree$node.label
write.tree(phy=tree, file="startingTree.phy")
## Leer el arbol inicial en Nexus
tree <- read.nexus("data/posterior_maxcred.tre")
## Establece la raiz
tree <- root(phy=tree, outgroup="Nematistius_pectoralis", resolve.root=TRUE)
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=c(sphyraenidae_id, carangidae_id, istiophoridae_id),
calibstring=NA)
## Guardemos el punto de calibracion
calib$calibstring[which(calib$id == "76")] <- "'L[0.72@0.001@0.01157324@0.025]'"
calib$calibstring[which(calib$id == "62")] <- "'SN[0.4502984@0.05403769@0.0]'"
calib$calibstring[which(calib$id == "56")] <- "'undefined'"
## Add node labels
tree$Nnode # 52 nodes - 53 tips
nodesnm <- (length(tree$tip.label)+1):(length(tree$tip.label)+tree$Nnode)
tree$node.label <- paste("n", nodesnm, sep ="")
## Remove brach lenghts for MCMCtree
tree$edge.length <- NULL
## unfortunately write.tree change "," to "-", so we have to
## change "," to another separator such as "@"
calib$id <- paste("n", calib$id, sep ="")
### Escribir el árbol de inicio con toda la información necesaria
for(i in calib$id){
tree$node.label[which(tree$node.label == i)] <- calib$calibstring[which(calib$id == i)]
}
write.tree(phy=tree, file="startingTree.phy")
## Crear un objeto para guardar los IDs y la informacion de los puntos de calibracion
calib <- data.frame(id=paste("n", c(sphyraenidae_id, carangidae_id, istiophoridae_id), sep=""),
calibstring=NA)
calib
calib
?
list.files
library(phangorn)
fpath <- "data/alignments"
## Podemos iterar sobre todos los alineamientos que tenemos en un directorio
alignms <- list.files(path=fpath, pattern="fas")
alignms
## Iterar los alineamientos
for(i in alignms){
## Read fasta
alg_i <- read.phyDat(paste(fpath, i, sep = "/"), format = "fasta")
## Write phylip
write.phyDat(alg_i, format = "phylip", colsep = "",
file = paste0(fpath, "/", i, ".phy"))
cat("Writing", i, "in phylip", "\n")
}
